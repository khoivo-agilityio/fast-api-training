# Exclude the local (macOS) virtual environment from the Docker build context.
# Without this, `COPY . .` overwrites the Linux .venv built in the builder stage.
.venv

# Python cache
__pycache__/
*.pyc
*.pyo
*.pyd
.pytest_cache/
.ruff_cache/
.coverage
htmlcov/

# Misc
.DS_Store
.git/
.env
*.md
docs/
